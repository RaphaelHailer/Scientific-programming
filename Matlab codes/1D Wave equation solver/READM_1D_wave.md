Code to solve the 1D wave equation using Dirichlet's boundary conditions (fixed values at extremities). The numerical method to solve the problem is the finite
difference method, with order of error O(h²).
